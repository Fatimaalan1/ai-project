public class Unwanted {
    Allocation allocation;
    Slot unwantedSlot;

    public Unwanted(Allocation inAllocation, Slot inUnwantedSlot)
    {
        allocation = inAllocation;
        unwantedSlot = inUnwantedSlot;
    }
}
