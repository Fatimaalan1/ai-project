public class PartialAssignment {
    Allocation allocation;
    Slot slot;

    public PartialAssignment(Allocation inAllocation, Slot inSlot)
    {
        slot = inSlot;
        allocation = inAllocation;
    }
}
